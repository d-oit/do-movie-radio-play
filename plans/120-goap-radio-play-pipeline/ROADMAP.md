# Radio Play Generation Pipeline — Roadmap

**Date:** 2026-06-22
**Status:** In Progress (workspace restructure complete)
**Goal:** Convert any movie into a complete radio play with AI-narrated scene descriptions

## Vision

Input: Movie file (MP4/MKV/WebM)
Output: Complete radio play audio (FLAC/MP3) that is:
- **100% of the original movie audio preserved** (dialogue, SFX, music, ambience)
- **AI narrator voice added** only where a listener would be lost without the picture
- Smooth insertion of narration into natural pauses (never cuts original audio)
- Quality that improves with every run (learns which moments need narration)

**This is automated Audiodeskription** — the narrator describes only what cannot be understood from audio alone.

## Architecture Overview

```
Movie File
    │
    ▼
┌─────────────────────────────────────────────────┐
│ GOAP Planner (ADR-120)                          │
│                                                 │
│  ┌─────────┐   ┌──────────┐   ┌─────────────┐  │
│  │ Decode  │──▶│ VAD/Tag  │──▶│ Visual Gap  │  │
│  │ (exist) │   │ (exists) │   │ Identifier  │  │
│  └─────────┘   └──────────┘   └──────┬──────┘  │
│                                       │         │
│  ┌─────────┐   ┌──────────┐   ┌──────▼──────┐  │
│  │ Quality │◀──│ Assembly │◀──│ Narrator    │  │
│  │ Verify  │   │ (insert) │   │ TTS (DE)    │  │
│  └────┬────┘   └──────────┘   └─────────────┘  │
│       │                                         │
│       ▼                                         │
│  ┌─────────┐                                    │
│  │ Learn   │ (ADR-122)                          │
│  └─────────┘                                    │
└─────────────────────────────────────────────────┘
    │
    ▼
Radio Play = Original Audio + Narrator Inserts
```

## Milestone Plan

### Milestone D: GOAP Core + Visual Gap Identification ✅ COMPLETE

**Goal:** Identify WHERE narration is needed — the core intelligence.

| Task | Duration | Output | Status |
|------|----------|--------|--------|
| GOAP planner with A* search | 3 days | `movie-radio-goap/src/planner.rs` | ✅ Done |
| World state + action registry | 2 days | `movie-radio-goap/src/lib.rs`, `actions.rs` | ✅ Done |
| Visual gap identifier algorithm | 5 days | `movie-radio-goap/src/gaps.rs` | ✅ Done |
| Gap context analyzer | 2 days | What came before/after, scene boundary detection | ✅ Done |
| CLI `radio-play --analyze-only` | 1 day | Shows identified gaps without synthesis | ✅ Done |

**Visual Gap Detection Signals (from existing pipeline):**
- Non-voice segments from VAD (candidates for narration)
- Segment tags: pure silence > 3s in dialogue context → likely visual scene
- Audio fingerprint discontinuity → scene change needing orientation
- Long ambience between dialogue blocks → "what's happening" gap
- Existing prompt data (already generated by `src/pipeline/prompts.rs`)

**Acceptance:**
- `timeline radio-play <MOVIE> --analyze-only` outputs gap list with timestamps + confidence ✅
- Not all non-voice segments are marked (only visually-critical ones) ✅
- False-positive rate tracked for learning ✅

### Milestone E: Narration Text Generation ✅ COMPLETE

**Goal:** Generate brief German descriptions for identified gaps.

| Task | Duration | Output | Status |
|------|----------|--------|--------|
| Narration text generator | 3 days | `movie-radio-goap/src/narrate.rs` | ✅ Done |
| Context-aware German descriptions | 2 days | Uses surrounding audio context | ✅ Done |
| Timing constraint: text must fit gap | 1 day | Word count bounded by gap duration | ✅ Done |
| Template fallback (no LLM needed) | 1 day | Deterministic mode from tags | ✅ Done |

**Narration rules:**
- Brief: max ~3 seconds of speech per gap (adjustable)
- German: all output in de-DE
- Contextual: describes only what's visually relevant
- Example: "Er betritt den Raum" / "Stille. Sie sieht ihn an." / "Schnitt: Außenaufnahme, Nacht"

**Acceptance:**
- Every identified gap gets a narration script ✅
- Text length matches available gap duration (at typical speech rate ~150 wpm DE) ✅
- Template mode works without LLM/API ✅

### Milestone F: Voice Synthesis 🔄 IN PROGRESS

**Goal:** Synthesize German narrator voice for each gap.

| Task | Duration | Output | Status |
|------|----------|--------|--------|
| VoiceSynthesizer trait | 1 day | `src/voice/mod.rs` | ✅ Done |
| Qwen3-TTS provider (primary DE) | 4 days | `src/voice/qwen3.rs` | 🔄 Stub (emotion prompts done) |
| Orpheus-3B German FT provider | 3 days | `src/voice/orpheus.rs` | 🔄 Stub (emotion tags done) |
| Kokoro provider (fallback) | 2 days | `src/voice/kokoro.rs` | 🔄 Stub (ONNX loading done) |
| ElevenLabs/OpenAI API providers | 1 day | `src/voice/elevenlabs.rs` | 🔄 Partial (HTTP works, no MP3 decode) |
| Fallback chain + model download CLI | 1 day | `src/voice/mod.rs` SynthesisOrchestrator | ✅ Done |
| Modal.com TTS provider | — | `src/voice/modal.rs` | ✅ Done (PR #110) |

**Acceptance:**
- Single consistent narrator voice across entire movie — NOT YET (most providers return silence)
- German pronunciation and prosody correct — NOT YET
- Emotion matches scene context — NOT YET
- Narration audio duration fits within identified gap — NOT YET

**What works:**
- Modal.com provider produces real audio via serverless GPU inference
- ElevenLabs makes real API calls (but response not decoded)
- Fallback chain tries providers in order
- Monthly spend tracking via learning database

### Milestone G: Radio Play Assembly ✅ COMPLETE (module level)

**Goal:** Insert narrator audio into original movie audio — produce final radio play.

| Task | Duration | Output | Status |
|------|----------|--------|--------|
| Assembly engine | 2 days | `movie-radio-goap/src/assemble.rs` | ✅ Done |
| Insert narration into gaps (no cutting) | 2 days | Preserve 100% original audio | ✅ Done |
| Crossfade narrator in/out (50-100ms) | 1 day | Smooth transitions | ✅ Done |
| Optional time-stretch for tight gaps | 1 day | Expand gap slightly if narration too long | 🔄 Not implemented |
| Output encoding (FLAC/MP3/WAV) | 1 day | Via ffmpeg or native | 🔄 Not wired to CLI |

**Key constraint:** Original audio is NEVER cut or removed. Narration is:
1. Inserted in natural pauses (preferred) ✅
2. Overlaid with volume ducking on background (if gap has ambience/music) ✅
3. Gap slightly time-stretched if narration doesn't fit (last resort) — NOT YET

**Acceptance:**
- Output plays without artifacts — Module works; not wired to CLI
- Zero overlap between narrator and original dialogue — ✅ (validation in assemble.rs)
- All original content audible and intact — ✅
- Duration within 10% of original — ✅

### Milestone H: Learning Loop 🔄 MOSTLY COMPLETE

**Goal:** System autonomously gets better at gap identification after every run.

| Task | Duration | Output | Status |
|------|----------|--------|--------|
| Execution trace recording | 1 day | Episodes in learning DB | ✅ Done (libsql database) |
| Automated self-evaluation metrics | 2 days | Overlap, timing, density scoring | 🔄 Partial (FP rate tracking done) |
| Autonomous gap identification improvement | 2 days | Pattern-based threshold adjustment | ✅ Done (adaptive thresholds) |
| Optional human feedback intake (review UI) | 1 day | High-weight correction signal | ✅ Done (review player) |
| Cross-run pattern accumulation | 1 day | Similar scenes share decisions | 🔄 Partial (fingerprinting done) |

**What works:**
- Adaptive thresholds auto-adjust based on false positive rate
- Calibration from correction records
- Learning database persists verification outcomes + spectral features
- Fingerprint matching for segment deduplication
- Profile-driven tag calibration (action/documentary/animation/drama)

**What's missing:**
- Cross-movie pattern similarity (no CSM/HDC vectors yet)
- Genre-aware adaptation
- Execution trace recording for narration quality

## Resource Requirements

### Local-Only Setup (Free)
- Disk: ~4GB for all models (Kokoro + Qwen3 + Orpheus)
- RAM: 4GB minimum (Kokoro), 8GB recommended (Qwen3), 16GB (Orpheus)
- CPU: Any x86-64 or ARM64 (AVX2 recommended)
- GPU: Optional, significantly speeds up Orpheus/Qwen3

### API Setup (Paid)
- ElevenLabs: $5-22/month depending on usage
- OpenAI TTS: ~$15 per hour of generated audio
- No local model storage needed

### Free Tier (Modal.com)
- $30/month free credits
- T4 GPU for Coqui XTTS v2
- CPU for Piper TTS
- ~500-1500 episodes per month on free tier

### Hybrid (Recommended)
- Modal for draft/preview (free, fast, GPU)
- ElevenLabs for final render (best quality)
- Budget cap prevents surprise costs

## Success Metrics

| Metric | Target | Current | Measurement |
|--------|--------|---------|-------------|
| Original audio preserved | 100% | ✅ 100% | Assembly module validates |
| Gap identification precision | >80% | ✅ 5-signal scoring | Automated quality checks |
| Narration timing | 0% overlap | ✅ 0% | Assembly validation |
| Narrator quality MOS | >3.5/5.0 | ❌ N/A | Most providers return silence |
| Self-learning improvement | >10% after 5 runs | 🔄 Partial | Adaptive thresholds work |
| Conversion time | <3x movie duration | ❌ N/A | Not wired end-to-end |
| Cost efficiency | <$2 per movie hour | ✅ ~$0.03 via Modal | Budget tracking works |

## Self-Learning Philosophy

The system is **fully autonomous by default**:
- Learns from its own execution traces, quality metrics, and pattern recognition
- Evaluates its own output (timing overlaps, silence detection, narration fit)
- Adjusts gap identification thresholds, narration density, and emotion mapping automatically
- **Human feedback is optional** — only used when explicitly provided (review UI corrections)
- The system never blocks on human input; it runs, evaluates, adapts, repeats

Human feedback (when given) is a **high-weight signal** that accelerates learning:
- "This gap didn't need narration" → immediately deprioritize that pattern
- "Narration is missing here" → immediately learn that gap pattern
- But the system continues improving without any human interaction

## Dependencies on Existing Pipeline

| Existing Component | Used By | Status |
|-------------------|---------|--------|
| `movie-radio-pipeline/` (VAD, tags) | Milestone D (scene context) | ✅ Complete |
| `movie-radio-learning/database.rs` | Milestone G (trace storage) | ✅ Complete |
| `movie-radio-types/config.rs` | All milestones (voice config) | ✅ Complete |
| `movie-radio-io/` (JSON, EDL, VTT) | Export narration scripts | ✅ Complete |
| `movie-radio-timeline/review.rs` | Milestone G (rating UI) | ✅ Complete |
| `config/profiles/` | Provider selection | ✅ Complete |

## External Crate Dependencies (d-o-hub org)

| Crate | Version | Use | Status |
|-------|---------|-----|--------|
| `do-memory-core` | 0.1.33 | Learning system (episodes, patterns, reward scoring) | Not yet integrated |
| `chaotic_semantic_memory` | 0.3.6 | Scene similarity search via HDC vectors | Not yet integrated |

## Language: German (de-DE) Primary

All narration output defaults to German. The scene description generator and TTS providers must:
- Generate description text in German ✅
- Synthesize speech with German pronunciation and prosody — NOT YET (providers return silence)
- Use German emotion descriptions for Qwen3-TTS voice control — ✅ (emotion prompts done)
- Support the German Orpheus fine-tune (`Orpheus-3b-German-FT-Q8_0.gguf`) — NOT YET
- Config override via `"language": "en"` for other locales — ✅

## Next Steps (Priority Order)

1. **Wire Modal provider into radio-play CLI** — already produces real audio
2. **Add MP3 decode for ElevenLabs** — second working provider
3. **Wire GOAP orchestrator to real pipeline stages** — connect planning to execution
4. **Wire radio-play CLI to full pipeline** — gap → narrate → TTS → assemble → output
5. **Implement local TTS inference** — Kokoro ONNX, Orpheus GGUF, Qwen3
6. **Add OpenAI TTS provider** — third cloud option

## Open Questions

1. **Gap detection without video**: Audio-only detection will miss some visual moments — acceptable for v1?
   - Proposal: Audio-only v1 with self-learning; optional video frame analysis (VLM) in v2
2. **Narration density**: How much narration is too much?
   - Proposal: Conservative default (only high-confidence gaps), self-adjusting via quality metrics
3. **Time-stretching**: Expanding gaps changes movie timing — acceptable?
   - Proposal: Max 500ms expansion per gap, prefer fitting narration to existing gap length
4. **Subtitle integration**: Use existing SRT for dialogue timing?
   - Proposal: Yes — existing `movie-radio-validation/src/srt.rs` parses subtitles, use as additional gap identification signal

## Experimental Status

This entire pipeline is **experimental** (2026). Expected evolution:
- TTS models improve rapidly — provider trait allows hot-swapping
- GOAP planning may be overkill if failure rates are low — can simplify to linear
- Learning system needs real-world data to validate improvement claims
- First viable end-to-end demo expected once Modal provider is wired to CLI
